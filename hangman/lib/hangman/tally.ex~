defmodule Hangman.Tally do
  def tally(game = %Hangman.Struct{}) do
    game
    |> Map.delete(:word)
    |> Map.delete(:__struct__)
  end

  def tally(_) do
    # when fed invalid data
    IO.puts "Invalid parameter here :( try a Hangman.Struct!"
  end
  
end
